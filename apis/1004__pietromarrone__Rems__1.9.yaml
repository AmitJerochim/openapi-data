openapi: 3.0.0
info:
  version: "1.9"
  title: Rems APi Rest
paths:
  "/schede/{scheda}":
    get:
      tags:
        - Scheda
      summary: Ritorna la Scheda
      operationId: getSchedaById
      parameters:
        - $ref: "#/components/parameters/schedaId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Scheda"
        "404":
          description: Scheda non trovata
  "/complessi/{complesso}":
    get:
      tags:
        - Complesso
      summary: Ritorna il Complesso
      operationId: getComplessoById
      parameters:
        - $ref: "#/components/parameters/complessoId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Complesso"
        "404":
          description: Complesso non trovato
  "/fabbricati/{fabbricato}":
    get:
      tags:
        - Fabbricato
      summary: Ritorna il Fabbricato
      operationId: getFabbricatoById
      parameters:
        - $ref: "#/components/parameters/fabbricatoId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Fabbricato"
        "404":
          description: Fabbricato non trovato
  "/aree/{area}":
    get:
      tags:
        - Area
      summary: Ritorna l'Area
      operationId: getAreaById
      parameters:
        - $ref: "#/components/parameters/areaId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Area"
        "404":
          description: Area non trovata
  "/terreni/{terreno}":
    get:
      tags:
        - Terreno
      summary: Ritorna il Terreno
      operationId: getTerrenoById
      parameters:
        - $ref: "#/components/parameters/terrenoId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Terreno"
        "404":
          description: Terreno non trovato
  "/utilizzi/{utilizzo}":
    get:
      tags:
        - Utilizzo
      summary: Ritorna l'Utilizzo
      operationId: getUtilizzoById
      parameters:
        - $ref: "#/components/parameters/utilizzoId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Utilizzo"
        "404":
          description: Utilizzo non trovato
  "/unita/{unitaImmobiliare}":
    get:
      tags:
        - Unita Immobiliare
      summary: Ritorna l'Unità Immobiliare
      operationId: getUnitaById
      parameters:
        - $ref: "#/components/parameters/unitaId"
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnitaImmobiliare"
        "404":
          description: Unità non trovata

components:
  parameters:
    schedaId:
      name: scheda
      in: path
      description: Codice Scheda
      required: true
      schema:
        type: string
    complessoId:
      name: complesso
      in: path
      description: Codice Complesso
      required: true
      schema:
        type: string
    fabbricatoId:
      name: fabbricato
      in: path
      description: Codice Fabbricato
      required: true
      schema:
        type: string
    areaId:
      name: area
      in: path
      description: Codice Area
      required: true
      schema:
        type: string
    unitaId:
      name: unitaImmobiliare
      in: path
      description: Codice Unità Immobiliare
      required: true
      schema:
        type: string
    terrenoId:
      name: terreno
      in: path
      description: Codice Terreno
      required: true
      schema:
        type: string
    utilizzoId:
      name: utilizzo
      in: path
      description: Codice Utilizzo
      required: true
      schema:
        type: string

  schemas:
    Scheda:
      type: object
      properties:
        codiceSceda:
          type: string
        codiceInterno:
          type: string
        tipo:
          $ref: "#/components/schemas/TipologicaNumber"
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        categoriaPatrimoniale:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        numero:
          type: object
          properties:
            complessi:
              type: integer
            fabbricati:
              type: integer
            unitaImmobiliari:
              type: integer
            unitaComuni:
              type: integer
            aree:
              type: integer
            terreni:
              type: integer
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
    Complesso:
      type: object
      properties:
        codiceComplesso:
          type: string
        codiceInterno:
          type: string
        attoCollegato:
          type: string
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        indirizzo:
          $ref: "#/components/schemas/Indirizzo"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        statoOccupazione:
          type: string
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        usoPrevalente:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        numero:
          type: object
          properties:
            fabbricati:
              type: integer
            unitaImmobiliari:
              type: integer
            unitaComuni:
              type: integer
            aree:
              type: integer
            terreni:
              type: integer
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        vincoli:
          type: array
          items:
            $ref: "#/components/schemas/Vincolo"
        pianiUrbanistici:
          type: array
          items:
            $ref: "#/components/schemas/PianoUrbanistico"
        allacci:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        accessi:
          type: array
          items:
            $ref: "#/components/schemas/Accesso"
        componentiImpiantistiche:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiArchitettonici:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        scarichi:
          type: array
          items:
            $ref: "#/components/schemas/Scarico"
        statoConservativo:
          type: array
          items:
            $ref: "#/components/schemas/StatoConservativo"
        recinsioni:
          type: array
          items:
            $ref: "#/components/schemas/Recinsione"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
    Fabbricato:
      type: object
      properties:
        codiceFabbricato:
          type: string
        codiceInterno:
          type: string
        attoCollegato:
          type: string
        tipoStruttura:
          $ref: "#/components/schemas/TipologicaString"
        numeroPianiFuoriTerra:
          type: integer
          format: int
        numeroPianiEntroTerra:
          type: integer
          format: int
        annoCostruzione:
          type: integer
          format: int
        epocaCostruzione:
          $ref: "#/components/schemas/TipologicaString"
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        indirizzo:
          $ref: "#/components/schemas/Indirizzo"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        statoOccupazione:
          type: string
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        rapportoGestione:
          $ref: "#/components/schemas/TipologicaString"
        categoriaPatrimoniale:
          $ref: "#/components/schemas/TipologicaString"
        usoPrevalente:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        numero:
          type: object
          properties:
            unitaImmobiliari:
              type: integer
            unitaComuni:
              type: integer
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        vincoli:
          type: array
          items:
            $ref: "#/components/schemas/Vincolo"
        pianiUrbanistici:
          type: array
          items:
            $ref: "#/components/schemas/PianoUrbanistico"
        allacci:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiVerticali:
          type: array
          items:
            $ref: "#/components/schemas/ElementoVerticale"
        componentiEdilizie:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        accessi:
          type: array
          items:
            $ref: "#/components/schemas/Accesso"
        componentiImpiantistiche:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiArchitettonici:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        scarichi:
          type: array
          items:
            $ref: "#/components/schemas/Scarico"
        statoConservativo:
          type: array
          items:
            $ref: "#/components/schemas/StatoConservativo"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
    Area:
      type: object
      properties:
        codiceArea:
          type: string
        codiceInterno:
          type: string
        attoCollegato:
          type: string
        tipo:
          $ref: "#/components/schemas/TipologicaNumber"
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        indirizzo:
          $ref: "#/components/schemas/Indirizzo"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        statoOccupazione:
          type: string
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        rapportoGestione:
          $ref: "#/components/schemas/TipologicaString"
        categoriaPatrimoniale:
          $ref: "#/components/schemas/TipologicaString"
        usoPrevalente:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        numero:
          type: object
          properties:
            fabbricati:
              type: integer
            unitaImmobiliari:
              type: integer
            unitaComuni:
              type: integer
            terreni:
              type: integer
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        vincoli:
          type: array
          items:
            $ref: "#/components/schemas/Vincolo"
        pianiUrbanistici:
          type: array
          items:
            $ref: "#/components/schemas/PianoUrbanistico"
        allacci:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        accessi:
          type: array
          items:
            $ref: "#/components/schemas/Accesso"
        componentiImpiantistiche:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        scarichi:
          type: array
          items:
            $ref: "#/components/schemas/Scarico"
        statoConservativo:
          type: array
          items:
            $ref: "#/components/schemas/StatoConservativo"
        attraversamenti:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        recinsioni:
          type: array
          items:
            $ref: "#/components/schemas/Recinsione"
        colture:
          type: array
          items:
            $ref: "#/components/schemas/Coltura"
        elementiArtificiali:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiNaturali:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        infrastrutture:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        stradeInterne:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
        beniNonEdificati:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaNonEdificato"
    UnitaImmobiliare:
      type: object
      properties:
        codiceUnita:
          type: string
        codiceInterno:
          type: string
        attoCollegato:
          type: string
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        indirizzo:
          $ref: "#/components/schemas/Indirizzo"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        statoOccupazione:
          type: string
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        rapportoGestione:
          $ref: "#/components/schemas/TipologicaString"
        categoriaPatrimoniale:
          $ref: "#/components/schemas/TipologicaString"
        destinazioneUso:
          $ref: "#/components/schemas/TipologicaString"
        usoPrevalente:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        vincoli:
          type: array
          items:
            $ref: "#/components/schemas/Vincolo"
        pianiUrbanistici:
          type: array
          items:
            $ref: "#/components/schemas/PianoUrbanistico"
        allacci:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        accessi:
          type: array
          items:
            $ref: "#/components/schemas/Accesso"
        elementiArchitettonici:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        statoConservativo:
          type: array
          items:
            $ref: "#/components/schemas/StatoConservativo"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
    Terreno:
      type: object
      properties:
        codiceTerreno:
          type: string
        codiceInterno:
          type: string
        attoCollegato:
          type: string
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        regione:
          $ref: "#/components/schemas/TipologicaNumber"
        provincia:
          $ref: "#/components/schemas/TipologicaString"
        comune:
          $ref: "#/components/schemas/TipologicaString"
        indirizzo:
          $ref: "#/components/schemas/Indirizzo"
        descrizione:
          type: string
        servizio:
          $ref: "#/components/schemas/TipologicaString"
        statoOccupazione:
          type: string
        tipoBeneDemanio:
          $ref: "#/components/schemas/TipologicaString"
        stato:
          enum:
            - VIGENTE
            - CESSATO
        rapportoGestione:
          $ref: "#/components/schemas/TipologicaString"
        categoriaPatrimoniale:
          $ref: "#/components/schemas/TipologicaString"
        destinazioneUso:
          $ref: "#/components/schemas/TipologicaString"
        usoPrevalente:
          $ref: "#/components/schemas/TipologicaString"
        annullamento:
          $ref: "#/components/schemas/TipologicaString"
        dataChiusura:
          type: string
          format: date-time
        stima:
          $ref: "#/components/schemas/Stima"
        canoneAnnuo:
          type: number
          format: double
        superficie:
          type: number
        costiGestione:
          $ref: "#/components/schemas/CostiGestione"
        contrattiVigenti:
          $ref: "#/components/schemas/NumeroContratti"
        contrattiCessati:
          $ref: "#/components/schemas/NumeroContratti"
        federalismo:
          enum:
            - NON_TRATTATO
            - TRATTATO
            - PARZIALMENTE_TRATTATO
        trasferibilitaFederalismoDemaniale:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        dimensioni:
          type: array
          items:
            $ref: "#/components/schemas/Dimensione"
        datiCatastali:
          type: array
          items:
            $ref: "#/components/schemas/Catasto"
        accessi:
          type: array
          items:
            $ref: "#/components/schemas/Accesso"
        statoConservativo:
          type: array
          items:
            $ref: "#/components/schemas/StatoConservativo"
        attraversamenti:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        recinsioni:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        colture:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiNaturali:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        elementiArtificiali:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        infrastrutture:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        stradeInterne:
          type: array
          items:
            $ref: "#/components/schemas/Caratteristica"
        valori:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaValore"
        caratteristicheNonEdificabili:
          type: array
          items:
            $ref: "#/components/schemas/CaratteristicaNonEdificato"
    Utilizzo:
      type: object
      properties:
        matricolaContratto:
          type: string
        descrizione:
          type: string
        rapporto:
          $ref: "#/components/schemas/Rapporto"
        dataPrimaOccupazione:
          type: string
          format: date
        dataFineOccupazione:
          type: string
          format: date
        raccolta:
          $ref: "#/components/schemas/Raccolta"
        dataDecorrenza:
          type: string
          format: date
        dataStipula:
          type: string
          format: date
        dataFineContratto:
          type: string
          format: date
        dataScadenza:
          type: string
          format: date
        durata:
          $ref: "#/components/schemas/Durata"
        preavvisoGiorni:
          type: number
        depositoCauzionale:
          type: number
          format: double
        normativaApplicata:
          $ref: "#/components/schemas/TipologicaString"
        naturaOccupazionale:
          $ref: "#/components/schemas/TipologicaString"
        ufficio:
          $ref: "#/components/schemas/TipologicaString"
        descrizioneAnagrafica:
          type: string
        tipoUtilizzatore:
          $ref: "#/components/schemas/TipologicaString"
        tipoUtilizzo:
          type: string
        statoRapporto:
          enum:
            - VIGENTE
            - CESSATO
        filiale:
          $ref: "#/components/schemas/TipologicaNumber"
        attiCollegati:
          type: array
          items:
            type: string
        rivalutazione:
          type: object
          properties:
            causale:
              $ref: "#/components/schemas/TipologicaString"
            importo:
              type: number
              format: double
            decorrenza:
              type: string
              format: date
            percentuale:
              type: number
            meseDecorrenza:
              type: string
            modalitaCalcolo:
              enum:
                - ASSOLUTO
                - RELATIVO
            periodicita:
              enum:
                - ANNUALE
                - BIENNALE
                - TRIENNALE
            modalitaSchedulazioneRivalutazione:
              enum:
                - ANNUALE
                - MENSILE
        regolarita:
          type: string
        numero:
          type: object
          properties:
            schede:
              type: integer
            complessi:
              type: integer
            fabbricati:
              type: integer
            unitaImmobiliari:
              type: integer
            unitaComuni:
              type: integer
            aree:
              type: integer
            terreni:
              type: integer
        canoni:
          type: array
          items:
            $ref: "#/components/schemas/Canone"
        rivalutazioni:
          type: array
          items:
            $ref: "#/components/schemas/Rivalutazione"
        soggetti:
          type: array
          items:
            $ref: "#/components/schemas/Soggetto"
        rappresentanteLegale:
          $ref: "#/components/schemas/RappresentanteLegale"

    TipologicaString:
      type: object
      properties:
        id:
          type: string
        descrizione:
          type: string
    TipologicaNumber:
      type: object
      properties:
        id:
          type: number
        descrizione:
          type: string
    NumeroContratti:
      type: object
      properties:
        diretti:
          type: integer
        indiretti:
          type: integer
    CostiGestione:
      type: object
      properties:
        totale:
          type: number
          format: double
        anno:
          type: number
        tutti:
          type: array
          items:
            type: object
            properties:
              tipo:
                type: string
              competenza:
                type: string
              anno:
                type: number
              importo:
                type: number
                format: double
    Dimensione:
      type: object
      properties:
        valore:
          type: number
          format: double
        tipo:
          type: string
        unitaMisura:
          type: string
    Catasto:
      type: object
      properties:
        comuneCatastale:
          type: string
        comuneUbicazione:
          type: string
        tipo:
          type: string
        partita:
          type: string
        foglio:
          type: string
        particelle:
          type: string
        subalterno:
          type: string
    CaratteristicaValore:
      type: object
      properties:
        tipo:
          type: string
        importo:
          type: number
          format: double
        dataRilevamento:
          type: string
          format: date
        dataInizio:
          type: string
          format: date
        dataFine:
          type: string
          format: date
    Stima:
      type: object
      properties:
        valore:
          type: number
          format: double
        data:
          type: string
          format: date-time
    Caratteristica:
      type: object
      properties:
        tipo:
          type: string
        numero:
          type: string
        note:
          type: string
    Indirizzo:
      type: object
      properties:
        indirizzo:
          type: string
        civico:
          type: string
        cap:
          type: string
    Vincolo:
      type: object
      properties:
        descrizione:
          type: string
        descrizioneCaratteristica:
          type: string
        note:
          type: string
        dataInizio:
          type: string
          format: date
        dataFine:
          type: string
          format: date
    PianoUrbanistico:
      type: object
      properties:
        tipo:
          type: string
        descrizione:
          type: string
        organoRilasciante:
          type: string
        livello:
          type: string
        denominazionePU:
          type: string
        prescrizione:
          type: string
        dataVisura:
          type: string
          format: date
    Accesso:
      type: object
      properties:
        indirizzo:
          type: string
        civico:
          type: string
        cap:
          type: string
        frazione:
          type: string
        localita:
          type: string
        principale:
          type: boolean
        tipo:
          type: string
        modalitaAccesso:
          type: string
        georiferito:
          type: boolean
        comune:
          type: string
    Scarico:
      type: object
      properties:
        tipo:
          type: string
        tipoSmaltimento:
          type: string
        numero:
          type: integer
        note:
          type: string
    StatoConservativo:
      type: object
      properties:
        tipo:
          type: string
        livello:
          type: string
        pericolo:
          type: integer
        note:
          type: string
    Recinsione:
      type: object
      properties:
        tipo:
          type: string
        note:
          type: string
    ElementoVerticale:
      type: object
      properties:
        tipo:
          type: string
        numero:
          type: integer
        descrizione:
          type: string
        principale:
          type: boolean
        interno:
          type: boolean
        sicurezza:
          type: string
        quote:
          type: string
    CaratteristicaNonEdificato:
      type: object
      properties:
        tipoTerreno:
          type: string
        andamentoOrografico:
          type: string
        morfologia:
          type: string
        fasciaAltimetrica:
          type: string
    Coltura:
      type: object
      properties:
        tipo:
          type: string
        note:
          type: string
    Raccolta:
      type: object
      properties:
        numero:
          type: number
        data:
          type: string
          format: date
    Durata:
      type: object
      properties:
        anni:
          type: number
        mesi:
          type: number
        giorni:
          type: number
    Canone:
      type: object
      properties:
        tipo:
          type: string
        importo:
          type: number
        dataInizio:
          type: string
          format: date
        dataFine:
          type: string
          format: date
    Rivalutazione:
      type: object
      properties:
        data:
          type: string
          format: date
        canoneOld:
          type: number
        canoneNew:
          type: number
        indice:
          type: number
        anno:
          type: number
        stato:
          type: string
    Soggetto:
      type: object
      properties:
        tipo:
          $ref: "#/components/schemas/TipologicaString"
        codiceFiscale:
          type: string
        patitaIva:
          type: string
        codicePagamenti:
          type: string
        denominazione:
          type: string
        recapito:
          type: string
        dataInizio:
          type: string
          format: date
    RappresentanteLegale:
      type: object
      properties:
        nome:
          type: string
        cognome:
          type: string
        ragioneSociale:
          type: string
        codiceFiscale:
          type: string
        partitaIva:
          type: string
        note:
          type: string
    Rapporto:
      type: object
      properties:
        codice:
          type: string
        tipo:
          type: string

# Added by API Auto Mocking Plugin
servers:
  - description: SwaggerHub API Auto Mocking
    url: https://virtserver.swaggerhub.com/pietromarrone/Rems/1.0.0
tags:
  - name: Scheda
  - name: Complesso
  - name: Fabbricato
  - name: Area
  - name: Unita Immobiliare
  - name: Terreno
  - name: Utilizzo